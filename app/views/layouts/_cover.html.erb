<div class="cover">
		<nav class="navbar bottom navbar-inverse navbar-inner container" role="navigation">
	  				<%= link_to "Bulldog Taxi", root_path, id: "logo" %>
						<ul class="nav pull-right edge">
							<% if @signedin && User.find_by(netid: session[:cas_user]).fname != nil %>
								<li><%= link_to "Create Trip", new_trip_path %></li>
							<% else %>
								<li><%= link_to "Create Trip", new_user_path %></li>
							<% end %>
							<li><%= link_to "About Us", about_path %></li>
							<% if @signedin && User.find_by(netid: session[:cas_user]).fname != nil %>
							<li id="fat-menu" class="dropdown">
              					<a href="#" class="dropdown-toggle" data-toggle="dropdown">
					                <%= User.find_by(netid: session[:cas_user]).fname.split(" ")[0] %> <b class="caret"></b>
              					</a>
              					<ul class="dropdown-menu">
					                <li><%= link_to "Profile", User.find_by(netid: session[:cas_user]) %></li>
					                <li class="divider"></li>
					                <li>
					                  <%= link_to "Sign out", logout_path %>
					                </li>
              					</ul>
           					</li>

							<% end %>
						</ul>
		</nav>
	</div>
	</div>