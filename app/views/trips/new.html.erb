<%= @user.name %>
<%= @user.email %>


<div class="container">
	<div class='span4'>
	</div>
	<div class='span4 hero-unit center'>
		<%= form_for @trip do |f| %>
			<%= f.label "Flight Number" %>
		  <%= f.text_field :flight_number %>

		  <%= f.hidden_field :confirmation, value: rand(100..200) %>

		  <%= f.submit "Next", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
	<div class='span4'>
	</div>
</div>