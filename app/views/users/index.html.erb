<div class="main-content container">
		<div class="hero-unit span12" >
			<h3> Users</h3>
			<% @users.each do |user| %>
				User: <%= user.fname + " " + user.lname %> |
				Tokens: <%= user.tokens %> |
				<br><br>
			<% end %>
					
		</div>
</div>